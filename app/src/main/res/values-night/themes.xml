<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BelfastWeather" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/shadow</item>
        <item name="colorPrimaryVariant">@color/shadow</item>
        <item name="colorOnPrimary">@color/shadow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/shadow</item>
        <item name="colorSecondaryVariant">@color/shadow</item>
        <item name="colorOnSecondary">@color/shadow</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>